const fs=require('node:fs');
const original=JSON.parse(fs.readFileSync(__dirname+'/mcp.archify.json'));
const domain={client:'client',direct:'client',stdio:'transport',https:'host',rpc:'host',dispatch:'host',operations:'host',guidance:'rules',binding:'rules',agent:'neutral',browser:'browser',widget:'browser',documents:'browser'};
const data={version:1,title:'PenEcho MCP 架构 · 本地自动布局',description:'同一份语义数据：模型提供实体、边界和关系；浏览器负责布局、连线和展示。',domains:[{id:'client',label:'调用方',color:'blue'},{id:'transport',label:'桥与传输',color:'teal'},{id:'host',label:'Node 宿主职责',color:'orange'},{id:'rules',label:'规则与绑定',color:'purple'},{id:'browser',label:'浏览器执行',color:'green'},{id:'neutral',label:'可选能力',color:'slate'}],groups:[{id:'host',label:'PenEcho Node 宿主 · 同一进程',domain:'host'},{id:'browser-scope',label:'已授权浏览器',domain:'browser'}],nodes:original.components.map(n=>({id:n.id,label:n.label,subtitle:n.sublabel,domain:domain[n.id],type:n.type,...(original.boundaries[0].wraps.includes(n.id)?{group:'host'}:original.boundaries[1].wraps.includes(n.id)?{group:'browser-scope'}:{})})),edges:original.connections.map(e=>({from:e.from,to:e.to,...(e.label?{label:e.label}:{}),...(e.variant==='dashed'?{kind:'optional'}:{})})),details:[{domain:'transport',title:'stdio 桥与发现',items:['轻量桥接进程，不启动完整 PenEcho 应用。','读取本机凭据与发现缓存，经 HTTPS /mcp 复用连接。','HTTP 空闲清理不终止仍打开 stdin 的桥；恢复连接不自动重放已提交的写请求。']},{domain:'host',title:'接入与画布分派',items:['HTTPS listener 负责认证与传输会话。RPC 校验工具输入，分派到目标浏览器与文档。','图片上传和工具调用共享 HTTPS 接入；不同端口候选不代表多台服务器。','共用画布操作管理版本、hash、幂等和虚拟文件读写。']},{domain:'rules',title:'规则与会话绑定',items:['guidance 是独立只读分支，不经过画布执行队列。','client/sessionKey 映射到 canvasId/documentId，持久绑定存储在 Node 侧。','浏览器连接 ID、文档 ID、业务 sessionId 和 HTTPS session ID 互不相同。']},{domain:'browser',title:'浏览器文档与渲染',items:['授权浏览器通过 WebSocket /api/mcp/canvas 接收 call，返回 result、截图和错误。','文档和快照由浏览器持有；Widget 在同一个沙箱 iframe 中渲染。','这张图的节点可点击查看职责与相关关系，主图可独立导出 SVG / PNG。']}],notes:['结果、错误与截图沿原请求通道返回；主图不重复画回程。Cloud 可选转发通道未展开。']};
for(const n of data.nodes)n.details=data.details.find(d=>d.domain===n.domain)?.items || [n.subtitle || n.label];
fs.writeFileSync(__dirname+'/mcp.semantic.json',JSON.stringify(data,null,2)+'\n');
