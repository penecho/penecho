# R8 geometry repair — geometry improved, color/capture defects remain

这是最新几何修正版的真实 MCP 截图。主干、分支方向、Node/浏览器所有权、独立图例、主图简化和标题基线已经正确，请保留这些成果。仅修正下面三个问题，保留其余正确的几何与事实；最终返回完整 HTML，不用 JSON 补丁。

1. 颜色仍按 A/B/C/D 类名轮换：主图浏览器绿色，但“浏览器画布运行时”详情变成紫色；“会话与绑定存储”反而是绿色。请真正使用架构规则里的语义领域 key（data 属性、语义 class 或同等做法），让图中领域与详情共享颜色。不要再依赖排列序号配色。跨领域内容可中性、拆分或分别用具名颜色标记，组织方式自主选择，但要能从图中颜色和名字快速找到详情。
2. “同一接入端点”的文本末尾碰到了转弯竖线，似乎用线段中点当作了文字的起点。请按完整文字框选择锚点和留白。“browser canvasCall”标签的白色底跨过两侧宿主框边，遮断了框线；应缩短/重新分配标签空间，不得用底色遮住其它边界。保持已有正确箭头端点和方向。
3. 详情的带背景代码标签跨行后，在 MCP 截图中出现大块空白，覆盖附近文字。DOM 测量显示 session-client.js 标签跨两行，其总包围框横跨大部分文本列。请去掉 .code 这类行内装饰背景/边框/内边距，让代码标识符以清晰可换行的普通等宽文字呈现；可调整断词，确保路径不会撑出详情列。必须再按真实宿主截图可读来交付，不能只看 HTML 源码。

修复由你完成，监督者没有给出坐标或手写 HTML。不要重画正确的主链，不要添加新的架构事实。主图与详情保留。
