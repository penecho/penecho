# R8 visual repair — rejected after MCP pixel review

这张是修正后经真实 MCP 再次渲染的截图。简化实体、guidance 箭头方向、浏览器边界、Cloud 放到详情都已改善，保留这些成果。但还有以下明确不合格项：
- 直接 HTTPS 客户端的箭头悬空：终点在 HTTPS MCP 接入节点左下方的空白处，没有接到实体轮廓。应实际接入同一个 HTTPS 接入节点，并让标签避开框边与节点。
- authoring-guidance 现在在 Node 宿主框之外，但它实际属于同一 Node 进程。外框必须包住它、主链路和存储/Agent职责，并留够标题与内边距。
- 共用画布操作与内置 Agent 的右边紧贴 Node 外框，没有内边距。挪动/扩大边界时同步维护跨框连线，不能只改框造成新碰撞。
- 同一主干中，前四个节点与后四个节点的标题/副标题基线有明显高低差。按相同文本槽统一对齐；同级节点标题字号和副标题字号一致。
- 关系标签有 12 或 12.5px，部分节点短职责太长。请在实际显示尺度保证可读，优先短标签、合理的通道间距；不要用微小字塞进空间。
- 颜色对应仍不准确：“传输与接入”是青色，但内含橙色 HTTPS 接入/端口/上传事实；“会话与绑定”为橙色却覆盖紫色持久存储；“规则分支”为紫色却又覆盖灰色内置 Agent。请按图中实体/领域颜色整理详情。可自主合并拆分栏目，不要求固定数量；跨领域内容可用中性色，名称要能从主图直接对应。

请修复这些原因并检查所有受影响连线、标签和所属边界。你自己计算坐标和留白；监督者没有提供布局坐标。保持事实准确、正交走线和独立详情。
