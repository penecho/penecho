# R8 — fresh output, MCP rendered, rejected

这是你的上一轮 HTML 经真实 PenEcho MCP 在 1939×1134 CSS px 视口渲染后的截图。请根据架构专属规则修正下列已确认问题，并自行检查剩余图形；可以调整构图和抽象，不要求保留原坐标、节点数或布局。修改由你完成，监督者只指出问题，不代写连线。

1. 主图 get_guidance 的箭头从 guidance 指向 RPC，方向反了。该调用由 RPC 分出只读分支读取规则；按本图的调用语义应 RPC → guidance。
2. CloudMcpBridge 是 Node 宿主内的桥，不是浏览器组件或第三方外部服务。当前图把它排在宿主外，归属不清；浏览器文档/Widget 没有明确的浏览器边界。按事实表达所有权。可选 Cloud 若妨碍主图可只在独立详情说明，不必强行画完所有旁路。
3. 文档与快照由浏览器文档运行时管理；现在从 Widget Host 指向文档存储会让人误以为 Widget 持有文档。请修正关联来源或使用保留正确语义的抽象。
4. 主要实体几乎都有名称+文件名+职责三行，违反简单实体要求。文件名、路径、端口、实现列表放到对应详情；主图只保留短名称与最多一行短职责。主图文字要在实际显示尺寸可读。
5. 直接 HTTPS 入口的描述越出实体边界；设备凭据标签挤在很短的 Cloud 连线间隙。检查全部文字的实际边界，给标签足够空间，不用小字号塞进去。
6. 返回结果用一条从浏览器直接绕回客户端的大边，与“沿原请求通道返回”有歧义；可以用一次清晰说明表达原路返回，不必另造捷径箭头。
7. 详情中的会话/存储和规则/可观测性混用同一种颜色。让主图与对应详情按同一实体或领域使用同一颜色变量、边框和名称；跨领域说明用中性色，不按 A/B/C/D 顺序轮换颜色。

保持：主干明确、正交走线、分支与主干区分、独立的图例与详情。实现上共享节点边界与连线路径的几何来源，避免改了节点却没改相连路线。结果须通过语义与几何两项验收。返回修正后的完整独立 HTML，不要解释或 Markdown 代码围栏。
