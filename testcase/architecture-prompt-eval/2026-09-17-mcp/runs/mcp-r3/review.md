# R3 — rejected (source and standalone-browser pixels)

Fresh generation; same facts/base/model/parameters; only scoped rule changed. Raw HTML retained, no hand repair. MCP connection remained unresponsive; this review used the exact HTML in a standalone browser and does not claim Canvas verification.

Improvements: main sequence is horizontal, binding store is now a side dependency, RPC/guidance branch is shown, ordinary links are orthogonal.

Failures:
- Opaque Node and browser frames are painted after entering links. They erase the bridge-to-ingress/direct-client routes and the final browser-entering segment. This is a drawing-order defect, not merely routing preference.
- The internal Agent reuse label and guidance label overlap. Cloud endpoint name and several implementation identifiers overflow compact boxes.
- Main entities still contain implementation paths and multiple detail lines at 11.5–15.5 SVG units. Uniform downscaling reduces readability further.
- Mixed-domain detail remains assigned a single domain's color.

R4 reorganizes the general rule into an authoring sequence, strengthens name-plus-one-role separation, and adds the universal SVG paint-order constraint. It does not supply PenEcho-specific positions or the previous HTML.
