# Context-order comparison — rejected

This run uses the exact R5 body, facts, base guidance and model parameters, but places guidance after facts in the user message instead of in the system message. The ordering/role change is intentional and separately labeled; it is not another rule-only trial.

The source still misroutes the direct HTTPS client to session/tool dispatch, bypassing ingress and RPC. Several long edge labels extend into adjacent node rectangles; the optional Cloud connection does not reach its destination box. Moving guidance later is not a demonstrated solution. Keep this negative result; do not rewrite the record as a passing example.

Next, a separately labeled model repair tests the required render→concrete feedback→patch workflow on R4 (the clearest earlier composition). No case-specific coordinates or handwritten HTML patch are supplied by the supervisor. A repaired result must not be described as an independent first-pass success.
