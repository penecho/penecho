# R5 — rejected; tighter wording did not reliably improve adherence

Fresh generation; same base/facts/parameters as R4. Source inspection confirms regression: most nodes still contain filenames plus several detail lines despite the two-slot rule; the optional Cloud bridge is put inside the browser frame and connected from Widget host. The two side-dependency boxes overlap in source geometry. No hand repair was made. Live MCP remains disconnected.

Do not conclude that more specific wording guarantees better diagrams. The next comparison keeps this exact R5 rule and facts, changing only how guidance is supplied: after the fact packet, closer to the point where real MCP tools return just-in-time guidance. This is a context-order/role comparison, not another controlled rule-only revision and not evidence of an already confirmed cause.
