# R4 — improved but rejected

Fresh generation from unchanged facts/base/model/parameters. Exact HTML reviewed in a standalone browser at the requested 1939×1134 viewport; live MCP surface is disconnected, so no Canvas verification is claimed.

- Main topology correctly separates the binding store and guidance branch. Boundary paint order no longer hides the main entering arrows. Orthogonal paths are much clearer.
- DOM text-bound inspection finds one overlapping SVG text pair, `JSON-RPC` and `guidance 只读分支` (45×15 CSS px). R3 had six detected pairs at the same viewport. This is a sample observation, not a reliability rate.
- Essential entity boxes still have multiple implementation lines; the long Cloud WSS label exceeds its box.
- The guidance detail panel uses orange while its main entity is purple. Arbitrary A–F detail organization persists.
- Optional Cloud connection stops at the browser frame instead of a concrete runtime endpoint, so optional-path geometry remains below the rule's acceptance standard.

R5 strengthens the general two-slot label contract, bounded text layout, separate adjacent-branch label lanes and one shared semantic palette for both map and detail. It contains no project-specific entities or coordinates.
