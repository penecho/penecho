# R2 — rejected (source and standalone-browser review; Canvas capture pending)

This is a fresh GLM-5.3-Flash generation from the same facts, base guidance and parameters as R1. Only the scoped architecture rule changed. Exact HTML was submitted without hand edits.

- Improvement: HTTPS ingress now leads through RPC before dispatch.
- Semantic failure: dispatch → persistent binding storage → common Canvas operations incorrectly turns a supporting store into a forwarding stage.
- Endpoint failure: the direct HTTPS client route ends at a frame/gap instead of the ingress node; the host/browser route uses frame edges instead of the actual operation/runtime entities.
- Standalone-browser pixels confirm sweeping cubic routes, crowded rotated connection labels, long optional-Cloud text overflowing its box, and small main-map text. Preview used the exact response HTML through a loopback-only static server. This does not establish successful MCP publication or sandbox compatibility; Canvas capture remains pending.
- The MCP publish request `mcp-architecture-r2-present-01` and its identical retry returned `request_cancelled`. A subsequent inspect and exact-document open also returned `request_cancelled`; directory listing still reports the original document online. Application outcome is unknown, not a verified publication failure. Do not duplicate the artifact or overwrite history.

R3 changes only general rules: distinguish control flow from data/support dependencies, use actual named endpoints across frames, prefer orthogonal routes by repositioning, reserve bounded text space. No old HTML or case-specific coordinates are provided to the model.
