以下是同一任务的第一次输出，已在 PenEcho MCP 1939×1134 视口实际渲染。请保留架构规则和给定事实，修正下列查实的问题，重新输出完整 HTML；布局仍由你决定。不要把这些审查意见或“错误示范”画进成品，也不要用正文解释代替修正连线。

1. HTTP API 和 Canvas AI 管线之间没有连接，AI 请求路径在主图断开。Canvas Agent 应使用自己的模型连接，图中却连到 Canvas AI 管线，改变了事实。
2. Widget 与 IndexedDB 是独立孤立框，主图又没有表示它们与画布的关系；选择合适抽象层级，连接必要关系或把实现组成移到详情。
3. WebSocket 用斜线横穿空隙，双向端点不在对应节点边界。MCP/启动连线交叉且标签叠在一起，Electron→Node 线穿过 CLI 框，产生错误的中间节点暗示。为这些边规划不穿过无关节点的独立通道，标签留出足够空间；由实际端口坐标推导路线。
4. 两个启动入口分别启动 Node，不是 Electron 与 CLI 相互调用。模型 API 是远程服务，CLI 后端是本地子进程，分组/名称不能混淆所属边界。
5. 1200px 上限和大块空白让 1939px 视口中的字很小。使用可用空间和清晰的主次字号，详情保持在主图外；无需逐项复述所有事实。
