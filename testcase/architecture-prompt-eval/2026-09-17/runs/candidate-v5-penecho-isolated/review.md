# Candidate v5 — PenEcho

Rendered unchanged at 1939 × 1134 CSS px in the second test canvas. **Rejected under the user's v5 hard conditions.**

Positive changes worth preserving: simple component names; browser/local/external frames; several direct horizontal entity connections; browser/local/external/startup colors repeated in detail-card accents. This is closer to the requested map vocabulary.

Remaining failures visible in `canvas.webp`:

- MCP Canvas-write label overlaps the AI return label near the browser; the backing obscures another relationship.
- MCP's first route segment is diagonal because the computed start is at y=436 while its first waypoint is at y=408, despite no topology reason for the diagonal.
- Main colors mostly map to detail groups, but orange storage nodes have no matching storage-colored detail group.
- Canvas Agent's model relationship is missing; the WebSocket target in the browser is Widget display, not the stated browser Agent channel at a consistent abstraction level.
- Page remains limited to 1240 px with small labels in the wide host viewport.

No hand correction was applied. Input/output: 1175/11118 tokens; 260.17 seconds. The paired commerce request exhausted 16000 output tokens without visible HTML, so it provides no visual generalization evidence.
