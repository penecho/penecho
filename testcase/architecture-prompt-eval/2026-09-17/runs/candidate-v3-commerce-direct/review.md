# Candidate v3 — branching commerce

MCP screenshot at 1939 × 1134 CSS px: **rejected**. The first viewport shows readable nodes and meaningful groups, but critical relationship and overlap defects remain. A complete score is not assigned to the below-viewport content.

- The model draws red service-to-service arrows with a 'not serial' cross marker. Negative source statements have been turned into visually asserted relationships.
- CDN is connected to the public load balancer, unsupported by the supplied design.
- Worker boxes overlap the service row; several edges cut through labels and nodes.
- The requested shared node/edge implementation was not used.
- Main content continues below the viewport; the screenshot is a viewport capture, not proof of missing lower content.
