# Candidate v3 — PenEcho

MCP screenshot at 1939 × 1134 CSS px: **3/10, rejected** (entities 1, boundaries 0, relationships 0, hierarchy 1, geometry 1).

- Did not use the suggested node/edge table or a shared rendering helper.
- The external-model frame contains a local subprocess and MathJax; a Canvas-document entity is incorrectly put in the Node service.
- HTTP POST label is attached to external-model → browser, not browser → Canvas AI.
- External MCP Agent points to service startup, not the MCP component.
- Several labels overlap; major relationships are missing despite correct descriptions in detail tables.
- Capture has transparent outer regions; black areas in the returned preview are not an authored dark theme.

The graph itself contradicts the supplied facts, so cosmetic improvement cannot qualify it.
