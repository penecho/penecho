# Candidate v2 — branching commerce

The unchanged GLM HTML was published through PenEcho Cloud MCP at 1939 × 1134 CSS px.
`canvas.webp` shows a blank topology region, so this is **rejected, visual scoring unavailable**.

Additional source checks:

- Extracted inline SVG fails XML parsing at line 54, column 11 because comments contain `--`. This is a plausible SVG serialization/capture incompatibility; the precise host rendering failure has not been isolated. Do not report the blank area as proof the browser's live SVG is also blank.
- Inventory Worker at x=1040..1190 connects vertically to x=1115 at the account database, rather than inventory database x=870..1000.
- Several database paths cross unrelated service nodes.
- Load balancer line terminates at x=480, while the Gateway starts at x=520; source-to-target endpoint is detached.
- Summary says three databases while four are drawn.

These semantic/geometry errors independently disqualify the artifact, regardless of the capture issue. Original output has not been repaired.
