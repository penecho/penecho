实际 MCP 截图审查：当前版本比前面的更清晰，大部分连线已正交。只修以下具体缺陷，保留有用的结构；不要把反馈本身写入图中。
1. 图中“经模型适配器调用”和返回箭头实际接到 HTTP API 框；事实要求 Canvas AI 管线调用模型后端。请让真实起止节点正确，可以适当移动对应节点使连接整齐。
2. 返回箭头的长标签挤进组件及框线，请缩成必要短标签，把解释移到对应详情，留出足够标签间距。
3. Canvas Agent 使用自己的模型连接，此关系在图中遗漏，请添加正确的实体连接，不要经 Canvas AI 管线。
4. MCP 读写 Canvas 箭头停在浏览器外侧，未接到 Canvas 实体边缘；请让箭头真正接上，避免与 WebSocket 的端口/线重叠。
5. 橙色主图缺少对应橙色详情。请建立清晰的颜色/名称对应，避免复制已有说明。模型 API 与本地 CLI 是两种后端，不能暗示 CLI 在云端。
请基于已有坐标调整必要图元、端口、通道与标签，不要重构整个页面。视口仍是1939×1134。
